export const siteStats = {
  categories: 20,
  assessments: 240,
  assessmentsPerCategory: 12,
  monitoredAttempts: null as number | null,     // [CONFIRM] owner to supply real figure; null = hide
  activeRecruiters: null as number | null,      // [CONFIRM]
};
